namespace GameEngine.Events
{
    /// <summary>
    /// Enum representing different types of events that can occur in the game engine.
    /// </summary>
    public enum EventType
    {
        Input,
        Collision,
    }
}
